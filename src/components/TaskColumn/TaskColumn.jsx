/* eslint-disable react/prop-types */

const TaskColumn = ({title, tasks}) => {
    return(
        <div>
             <p> {title} </p>

             {/* tasks */}
             <div>
              
             </div>
        </div>
    )}
export default TaskColumn;